<%= form_for(@remote_project) do |f| %>
  <% if @remote_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@remote_project.errors.count, "error") %> prohibited this remote_project from being saved:</h2>

      <ul>
      <% @remote_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
